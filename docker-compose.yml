services:
  mirofish:
    image: ghcr.io/666ghj/mirofish:latest
    # 이미지 가속(가져오기가 느리면 위 주소를 대체할 수 있음)
    # image: ghcr.nju.edu.cn/666ghj/mirofish:latest
    container_name: mirofish
    env_file:
      - .env
    ports:
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads
